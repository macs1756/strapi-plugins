'use strict';

/**
 * test-slug-generate router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::test-slug-generate.test-slug-generate');
