'use strict';

/**
 * test-slug-generate service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::test-slug-generate.test-slug-generate');
